## Preferred CLI Tools

When working in the codebase, use these modern CLI tools:

- **fd**: Use `fd` instead of `find` for file searching
- **rg** (ripgrep): Use `rg` instead of `grep` for code searching
- **Grep tool**: Prefer using the built-in Grep tool over bash `grep` or `rg` commands
